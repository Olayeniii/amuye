const data = await fetch("./demo-data.json").then((response) => { if (!response.ok) throw new Error("Execution evidence could not be loaded"); return response.json(); });
const app = document.querySelector("#app");
const labels = { viability_onchain: "Viability", risk_synthesis: "Risk synthesis", security_analysis: "Security analysis" };
const safe = (value) => String(value ?? "").replace(/[&<>"']/g, (character) => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[character]));
const shortAddress = (value) => `${value.slice(0,8)}...${value.slice(-6)}`;
const money = (value) => Number(value).toLocaleString(undefined,{maximumFractionDigits:3});
const protocolSlug = (value) => String(value||"").trim().toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"");
let liveJob=null, pollTimer=null, historyJobs=null, historyLoading=false, historyError=null;
function statusIcon(status){return status==="completed"?"✓":status==="skipped"?"−":status==="cancelled"?"×":"•";}
function graph(mode){const roleById=Object.fromEntries(mode.graph.map(n=>[n.id,labels[n.role]]));return mode.graph.map((n,i)=>`<article class="node node-${safe(n.status)}"><div class="node-index">0${i+1}</div><div class="node-body"><div class="node-title"><h3>${safe(labels[n.role])}</h3><span class="status"><i>${statusIcon(n.status)}</i>${safe(n.status)}</span></div><dl class="node-meta"><div><dt>Depends on</dt><dd>${n.dependencies.length?n.dependencies.map(id=>safe(roleById[id]||id)).join(", "):"None"}</dd></div><div><dt>Gate</dt><dd>${safe(n.gate||"No continuation gate")}</dd></div><div><dt>Cost</dt><dd>${money(n.cost)} budget units</dd></div></dl><p class="reason">${safe(n.reason||"Baseline node, no graph mutation")}</p></div></article>`).join("");}
function evidence(mode){return mode.evidence.map(item=>{const facts=item.metrics?[`TVL $${money(item.metrics.currentTvlUsd)}`,`${item.metrics.chainCount} chain`,`${item.metrics.auditCount} audits`]:item.identifiedRisks?[`${item.identifiedRisks.length} material risks`,`Security gate: ${item.continueToSecurity}`]:[`Assessment: ${item.assessment}`,`${item.findings?.length||0} findings`];return `<div class="evidence-row"><strong>${safe(labels[item.role])}</strong><span>${facts.map(safe).join(" · ")}</span></div>`;}).join("");}
function jobSummary(mode){return `${mode.intent?`<section class="intent-strip"><span>Selected intent</span><strong>${safe(mode.intent.intent.replaceAll("_"," "))}</strong><small>${mode.intent.requiredCapabilities.map(item=>safe(labels[item]||item)).join(" · ")}</small><p>${safe(mode.intent.reason)}</p></section>`:""}<section class="job-strip"><div class="job-objective"><span>Objective</span><strong>${safe(mode.request.objective)}</strong></div><div><span>Budget</span><strong>${money(mode.request.maxBudget)} units</strong></div><div><span>Deadline</span><strong>${safe(new Date(mode.request.deadline).toLocaleDateString())}</strong></div><div><span>Priority</span><strong>${safe(mode.request.priority)}</strong></div><div class="constraints"><span>Hard constraints</span><strong>${mode.request.hardConstraints.map(item=>`<em>${safe(item)}</em>`).join("")}</strong></div></section>`;}
function memorySourcePanel(memory){const source=memory.source;if(!memory.enabled)return `<div class="memory-source unavailable"><strong>View Sibyl source</strong><span>No Sibyl memory read for this run.</span></div>`;if(!source)return `<div class="memory-source unavailable"><strong>View Sibyl source</strong><span>This is reproducible artifact evidence, not a live Sibyl response. Run a Memory ON assessment to inspect its source record.</span></div>`;if(!source.readPerformed||!source.records.length)return `<details class="memory-source"><summary>View Sibyl source</summary><div class="source-empty">${safe(source.message)}</div></details>`;return `<details class="memory-source"><summary>View Sibyl source</summary><div class="source-session"><span>Process</span><code>${safe(source.processSessionId)}</code><span>Sibyl source</span><code>${safe(source.databaseSource)}</code><span>Returned</span><code>${safe(source.returnedLessonIds.join(", "))}</code><span>Applied by plan</span><code>${safe(source.plannerMemoryRefs.join(", ")||"None")}</code></div>${source.records.map(l=>`<div class="source-record"><div><span>Lesson ID</span><code>${safe(l.id)}</code></div><div><span>Task pattern</span><strong>${safe(l.taskPattern)}</strong></div><div class="wide"><span>Learned strategy</span><p>${safe(l.strategy)}</p></div><div class="wide"><span>Reasoning</span><p>${safe(l.reasoning)}</p></div></div>`).join("")}</details>`;}
function visibleLesson(mode){if(mode.memory?.lessonRecord)return{lesson:mode.memory.lessonRecord,state:"recalled"};if(mode.learning?.lesson)return{lesson:mode.learning.lesson,state:"learned"};return null;}
function lessonSummary(mode){const v=visibleLesson(mode);if(!v)return"";return `<div class="source-record"><div class="wide"><span>${v.state==="recalled"?"What Amúyẹ recalled":"Lesson learned this run"}</span><p>${safe(v.lesson.strategy)}</p></div><div class="wide"><span>Why</span><p>${safe(v.lesson.reasoning)}</p></div></div>`;}
function memoryOutcome(mode){if(!mode.memory?.enabled)return"";const risk=mode.evidence?.find(i=>i.role==="risk_synthesis"),security=mode.graph?.find(n=>n.role==="security_analysis");if(mode.memory.adapted&&risk?.continueToSecurity===true&&security?.status==="completed")return `<div class="source-decision"><span>Outcome this run</span><p>Memory changed when downstream work was committed, but current risk evidence still justified security analysis. The final spend therefore remained ${money(mode.execution.spent)} budget units.</p></div>`;if(mode.memory.adapted&&risk?.continueToSecurity===false&&security?.status==="skipped")return `<div class="source-decision"><span>Outcome this run</span><p>Memory made security conditional, current risk evidence did not justify it, and the security purchase was skipped.</p></div>`;return"";}
function memoryConsequencePanel(mode){if(!["memory-off","memory-on"].includes(mode.key))return"";const p=data.memoryConsequence,off=p.withoutExperience,on=p.withExperience;return `<section class="memory-consequence panel"><div class="section-head"><div><p class="eyebrow">What Sibyl changed</p><h2>Prior execution experience changed one purchase</h2></div><span>Same task · same budget · same specialists</span></div><div class="consequence-grid"><article class="consequence-run ${mode.key==="memory-off"?"active":""}"><p>No prior execution experience</p><strong>${safe(off.specialistsCommissioned)} specialists commissioned</strong><dl><div><dt>Spend</dt><dd>${money(off.spend)} / ${money(off.budget)} budget units</dd></div></dl></article><article class="consequence-run recalled ${mode.key==="memory-on"?"active":""}"><p>Experience recalled from Sibyl</p><strong>${safe(on.specialistsCommissioned)} specialists commissioned</strong><dl><div><dt>Spend</dt><dd>${money(on.spend)} / ${money(on.budget)} budget units</dd></div></dl></article></div><div class="consequence-result"><strong>${safe(p.avoidedSpecialistCount)} unnecessary specialist purchase avoided</strong><strong>${money(p.preservedBudget)} budget units preserved</strong></div></section>`;}
function memoryCausalStrip(mode){if(mode.key!=="memory-on")return"";const p=data.memoryConsequence.withExperience;return `<section class="causal-strip"><span>Experience recalled from Sibyl<small>${safe(p.recalledLessonId)}</small></span><i>→</i><span>Security became conditional<small>${safe(p.securityGate)}</small></span><i>→</i><span>Risk evidence did not justify deeper work</span><i>→</i><span>${money(data.memoryConsequence.preservedBudget)} budget units preserved</span></section>`;}
function modePage(mode){const comparison=["memory-off","memory-on"].includes(mode.key)?`<div class="comparison-bar"><span>Controlled difference</span><b class="${mode.key==="memory-off"?"selected":""}">Memory OFF · security purchased · 95 units</b><b class="${mode.key==="memory-on"?"selected":""}">Memory ON · security skipped · 35 units</b></div>`:"";const visible=visibleLesson(mode),heading=!mode.memory.enabled?"No prior execution experience":mode.memory.lessonId?"Experience applied":visible?.state==="learned"?"Lesson learned":"No applicable experience recalled";return `<main><section class="result-hero"><div><p class="eyebrow">${safe(mode.eyebrow)}</p><h1>${safe(mode.result)}</h1><p class="verified"><i>✓</i>${safe(mode.verification)}</p></div><div class="spend-orb"><small>Spent</small><strong>${money(mode.execution.spent)}</strong><span>budget units</span><span>${money(mode.execution.remaining)} remaining</span></div></section>${comparison}${memoryConsequencePanel(mode)}${memoryCausalStrip(mode)}${jobSummary(mode)}<div class="main-grid"><section class="panel graph-panel"><div class="section-head"><div><p class="eyebrow">Decision path</p><h2>Execution graph</h2></div><span>${mode.execution.order.length} purchases</span></div><div class="graph">${graph(mode)}</div></section><aside class="side-stack"><section class="panel memory-panel"><div class="panel-label"><span class="signal ${mode.memory.enabled?"on":""}"></span>Sibyl memory ${mode.memory.enabled?"ON":"OFF"}</div><h2>${safe(heading)}</h2><dl class="stacked-list"><div><dt>Recalled lesson</dt><dd>${safe(mode.memory.lessonId||"None")}</dd></div><div><dt>Applicability</dt><dd>${safe(mode.memory.applicability)}</dd></div><div><dt>Rule influenced</dt><dd>${safe(mode.memory.influencedRule)}</dd></div></dl>${lessonSummary(mode)}${memoryOutcome(mode)}${memorySourcePanel(mode.memory)}</section><section class="panel summary-panel"><div class="panel-label">Execution summary</div><div class="metric-row"><div><span>Spent, units</span><strong>${money(mode.execution.spent)}</strong></div><div><span>Unspent, units</span><strong>${money(mode.execution.remaining)}</strong></div></div></section></aside></div><div class="lower-grid"><section class="panel"><div class="section-head"><div><p class="eyebrow">Accepted outputs</p><h2>Evidence</h2></div></div>${evidence(mode)}</section><section class="panel learning-panel"><div class="section-head"><div><p class="eyebrow">Outcome to memory</p><h2>Learning record</h2></div></div><p>${safe(mode.learning.reflection)}</p></section></div>${liveAcpPanel(mode)}</main>`;}
function liveAcpPanel(mode){if(!mode.acp||mode.acp.providerMode!=="live Virtuals ACP risk purchase on Base mainnet")return"";const jobs=mode.acp.providerJobs||[],proofs=mode.acp.settlementProofs||[];return `<section class="panel live-acp-panel"><div class="section-head"><div><p class="eyebrow">Paid specialist procurement</p><h2>Virtuals ACP on Base</h2></div><span>${jobs.length?"VERIFIED LIVE RUN":"NO ACP JOB CREATED"}</span></div>${jobs.map(job=>{const proof=proofs.find(p=>String(p.jobId)===String(job.acpJobId));return `<div class="acp-job"><div class="proof-grid"><div><span>ACP job</span><strong>${safe(job.acpJobId)}</strong></div><div><span>Provider</span><strong>${safe(shortAddress(job.providerId))}</strong></div><div><span>Status</span><strong>${safe(job.status)}</strong></div></div>${proof?`<div class="transactions"><a href="${safe(proof.funding.explorerUrl)}" target="_blank"><span>Funding</span><code>${safe(proof.funding.transactionHash)}</code></a><a href="${safe(proof.completion.explorerUrl)}" target="_blank"><span>Completion</span><code>${safe(proof.completion.transactionHash)}</code></a></div>`:""}</div>`;}).join("")}</section>`;}
function newAssessmentPage(){const d=new Date(Date.now()+86400000).toISOString().slice(0,16);return `<main><section class="result-hero form-hero"><div><p class="eyebrow">Live procurement run</p><h1>Commission a protocol assessment</h1><p class="verified">Amúyẹ owns planning, specialist selection, gates, evaluation, and learning.</p></div></section><form id="assessment-form" class="panel assessment-form"><label><span>Protocol</span><input name="protocolName" value="Aave" required></label><label><span>Protocol slug</span><input name="protocolSlug" value="aave" required></label><label class="wide"><span>Objective</span><textarea name="objective" required>Assess protocol Aave for integration viability and material risks</textarea></label><label><span>Maximum budget, units</span><input name="maxBudget" type="number" min="1" step="0.1" value="100" required></label><label><span>Deadline</span><input name="deadline" type="datetime-local" value="${d}" required></label><label><span>Priority</span><select name="priority"><option>balanced</option><option>risk</option><option>urgent</option></select></label><label><span>Client ID</span><input name="clientId" value="ui-demo-client" required></label><label class="wide"><span>Additional hard constraints, one per line</span><textarea name="hardConstraints"></textarea></label><label class="memory-choice"><input name="memoryEnabled" type="checkbox" checked><span>Use Sibyl operational memory</span></label><label class="memory-choice live-acp-choice"><input name="liveAcpEnabled" type="checkbox"><span>Live ACP risk purchase</span></label><p id="assessment-error" class="assessment-error" hidden></p><div class="form-actions"><button type="submit">Run assessment</button></div></form></main>`;}
function eventLabel(e){return ({request_accepted:"Request accepted",intake_accepted:"Intake validated",memory_retrieval_started:"Sibyl retrieval",plan_created:"Plan created",graph_mutation:"Graph updated",specialist_purchase:`Purchased ${labels[e.role]||e.role}`,acp_purchase_started:"Virtuals ACP purchase started",acp_job_completed:`ACP job ${e.acpJobId} completed`,settlement_verified:`Base settlement verified for ACP job ${e.acpJobId}`,execution_completed:"Result ready",execution_failed:"Execution failed"})[e.type]||e.type;}
function liveProgressPage(job){if(job.result)return modePage(liveResultToMode(job.result));return `<main><section class="result-hero"><div><p class="eyebrow">Live run</p><h1>${job.error?"Assessment failed":"Amúyẹ is executing the assessment"}</h1><p class="verified">${safe(job.error||"Polling real backend state")}</p></div></section>${job.request?jobSummary({request:job.request}):""}<section class="panel timeline-panel"><div class="section-head"><div><p class="eyebrow">Backend events</p><h2>Live execution</h2></div><span>${job.events.length} events</span></div><div class="timeline">${job.events.map(e=>`<div><i></i><strong>${safe(eventLabel(e))}</strong><span>${safe(new Date(e.at).toLocaleTimeString())}</span></div>`).join("")}</div></section></main>`;}
function liveResultToMode(r){const nodes=r.execution.nodes,outputs=r.finalResult.evidence,recalled=r.memory.recalledLessonIds,records=r.memory.source?.records||[],record=recalled.length?records.find(l=>l.id===recalled[0])||null:null;return {eyebrow:"Live procurement result",result:r.finalResult.decision,verification:r.finalResult.verification,request:r.request,intent:r.strategy.objectiveIntent,memory:{enabled:r.memory.enabled,lessonId:recalled[0]||null,lessonRecord:record,applicability:r.memory.applicability,influencedRule:recalled.length?r.strategy.rationale:"No operational lesson influenced this plan.",adapted:r.strategy.source==="adapted",source:r.memory.source},graph:nodes.map(n=>({id:n.id,role:n.type,status:n.status,dependencies:n.dependencies,gate:n.conditionalTrigger,cost:n.actualCost,reason:n.mutationReason})),execution:{purchased:r.execution.purchasedRoles.map(role=>({role})),order:r.execution.purchasedRoles,spent:r.execution.spent,remaining:r.execution.remainingBudget,stoppedEarly:r.execution.stoppedEarly,stopReason:r.execution.stopReason},evidence:outputs,learning:{reflection:[...r.reflection.successfulDecisions,...r.reflection.failedDecisions].join(" ")||"Execution evaluated before reflection.",lesson:r.lessonUpdate?.lesson||null},acp:{providerMode:r.providerMode,providerJobs:r.execution.providerJobs,settlementProofs:r.settlementProofs}};}
async function pollJob(url){const response=await fetch(url);liveJob=await response.json();if(location.hash==="#live-run")render();if(!["completed","failed"].includes(liveJob.status))pollTimer=setTimeout(()=>pollJob(url),450);}
function proofPage(){const p=data.partnerProof;return `<main><section class="result-hero proof-hero"><div><p class="eyebrow">Verified partner execution</p><h1>Risk synthesis was purchased through Virtuals ACP and settled on Base.</h1></div><div class="spend-orb"><small>ACP job</small><strong>${safe(p.jobId)}</strong><span>${safe(p.network)}</span></div></section><section class="panel proof-panel"><div class="proof-heading"><h2>riskSynthesis settlement</h2></div></section></main>`;}
function historyPage(){if(historyLoading)return `<main><section class="result-hero"><div><p class="eyebrow">Operational record</p><h1>Assessment history</h1><p class="verified">Loading persisted jobs…</p></div></section></main>`;if(historyError)return `<main><section class="result-hero"><div><p class="eyebrow">Operational record</p><h1>Assessment history</h1><p class="assessment-error">${safe(historyError)}</p></div></section></main>`;const jobs=historyJobs||[];return `<main><section class="result-hero"><div><p class="eyebrow">Operational record</p><h1>Assessment history</h1><p class="verified">Persisted separately from Sibyl learning memory.</p></div><div class="spend-orb"><small>Stored</small><strong>${jobs.length}</strong><span>recent jobs</span></div></section><section class="panel timeline-panel"><div class="section-head"><div><p class="eyebrow">Recent assessments</p><h2>Job history</h2></div><span>Survives restart</span></div><div class="timeline">${jobs.length?jobs.map(j=>`<div class="history-row" data-job="${safe(j.id)}"><i></i><strong>${safe(j.request?.objective||j.id)}</strong><span>${safe(j.status)} · ${j.memoryEnabled?"Sibyl ON":"Sibyl OFF"} · ${j.providerMode==="live_acp"?"Live ACP":"Local"}</span><small>${safe(new Date(j.createdAt).toLocaleString())}</small><button type="button" data-open-job="${safe(j.id)}">Open</button></div>`).join(""):`<p>No assessments have been persisted yet.</p>`}</div></section></main>`;}
async function loadHistory(){historyLoading=true;historyError=null;render();try{const r=await fetch("/api/assessments?limit=50");const v=await r.json();if(!r.ok)throw new Error(v.error||"Assessment history could not be loaded");historyJobs=v.assessments||[];}catch(e){historyError=e.message;}finally{historyLoading=false;render();}}
async function openHistoryJob(id){const r=await fetch(`/api/assessments/${encodeURIComponent(id)}`);const j=await r.json();if(!r.ok){historyError=j.error||"Assessment could not be loaded";render();return;}liveJob=j;location.hash="live-run";}
function render(){const route=location.hash.slice(1)||"memory-on",mode=data.modes.find(i=>i.key===route)||data.modes[1];const content=route==="new-assessment"?newAssessmentPage():route==="live-run"?(liveJob?liveProgressPage(liveJob):newAssessmentPage()):route==="history"?historyPage():route==="partner-proof"?proofPage():modePage(mode);const evidenceRoute=data.modes.some(i=>i.key===route)||route==="partner-proof";app.innerHTML=`<header><a class="brand" href="#memory-on"><img src="./assets/amuye-logo.png" alt="Amúyẹ"/><span><strong>Amúyẹ</strong><small>Procurement intelligence</small></span></a><nav><a class="new-action ${["new-assessment","live-run"].includes(route)?"active":""}" href="#new-assessment">New Assessment</a><a class="${route==="history"?"active":""}" href="#history">History</a>${data.modes.map(i=>`<a class="${route===i.key?"active":""}" href="#${i.key}">${safe(i.label)}</a>`).join("")}<a class="${route==="partner-proof"?"active":""}" href="#partner-proof">Partner proof</a></nav><span class="live"><i></i>${evidenceRoute?"Reproducible proof":"Live backend"}</span></header>${content}<footer><span>Amúyẹ</span><p>Procurement decisions shaped by execution evidence.</p></footer>`;
const form=document.querySelector("#assessment-form");if(form){const name=form.elements.protocolName,slug=form.elements.protocolSlug,obj=form.elements.objective;let last=obj.value,manual=false;slug.addEventListener("input",()=>manual=true);name.addEventListener("input",()=>{const n=name.value.trim();if(!manual)slug.value=protocolSlug(n);if(!obj.value.trim()||obj.value===last){last=`Assess protocol ${n} for integration viability and material risks`;obj.value=last;}});form.addEventListener("submit",async event=>{event.preventDefault();const fd=new FormData(form),s=protocolSlug(fd.get("protocolSlug"));if(!s)return;const req={objective:String(fd.get("objective")).trim(),maxBudget:Number(fd.get("maxBudget")),deadline:new Date(fd.get("deadline")).toISOString(),priority:fd.get("priority"),hardConstraints:[`protocolSlug=${s}`,...String(fd.get("hardConstraints")||"").split("\n").map(x=>x.trim()).filter(Boolean)],clientId:fd.get("clientId"),taskClass:"protocol_assessment"};form.querySelector("button").disabled=true;try{const memory=fd.get("memoryEnabled")?"on":"off",live=Boolean(fd.get("liveAcpEnabled"));let q="provider=local";if(live){const cr=await fetch("/api/acp/config"),c=await cr.json();if(!cr.ok||!c.enabled)throw new Error("Live ACP is not configured");if(!confirm(`Create a real paid Virtuals ACP job?\nOffering: ${c.offering}\nNetwork: ${c.network}\nMaximum expected spend: ${money(c.maxExpectedSpend)} ${c.asset}`))throw new Error("Live ACP purchase was not confirmed.");q="provider=live_acp&confirmAcp=true";}const r=await fetch(`/api/assessments?memory=${memory}&${q}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(req)}),a=await r.json();if(!r.ok)throw new Error(a.error||"Assessment was rejected");liveJob={id:a.jobId,status:a.status,providerMode:live?"live_acp":"local",request:req,events:[],result:null,error:null};historyJobs=null;location.hash="live-run";pollJob(a.statusUrl);}catch(e){const box=form.querySelector("#assessment-error");box.textContent=e.message;box.hidden=false;form.querySelector("button").disabled=false;}});}document.querySelectorAll("[data-open-job]").forEach(b=>b.addEventListener("click",()=>openHistoryJob(b.dataset.openJob)));}
addEventListener("hashchange",()=>{if(location.hash==="#history"&&historyJobs===null&&!historyLoading)loadHistory();else render();});render();if(location.hash==="#history")loadHistory();